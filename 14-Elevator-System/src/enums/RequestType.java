package enums;

/**
 * Represents the type of elevator request.
 */
public enum RequestType {
    EXTERNAL,  // Request from floor button (UP/DOWN)
    INTERNAL   // Request from inside elevator (destination floor)
}


